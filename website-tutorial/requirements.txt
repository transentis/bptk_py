BPTK-Py[plotting,xmile]
# Pinned, because the version decides what the site *is*: the marimo release determines
# which islands runtime the pages embed, and therefore which bundle patch_islands mirrors
# and patches - 437 files at 0.24.0, where 0.23.13 mirrored 405. Unpinned, CI and a laptop
# resolve differently and render the same sources into two different websites. Upgrading is
# a deliberate step: change this line, re-render, check the gate, and confirm the patch
# still applies.
marimo==0.24.0
